<%= form_for([@meal.restaurant, @meal, @comment]) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <p><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</p>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions waves-effect waves-light btn btn-small green darken-2">
    <%= f.submit %>
  </div>
<% end %>
