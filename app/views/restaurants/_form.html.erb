<%= form_for @restaurant, html: { multipart: true } do |f| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <p><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</p>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, class:"search-label" %>
    <%= f.text_field :name, class:"search-field" %>
  </div>

  <div class="field">
    <%= f.label :address, class:"search-label" %>
    <%= f.text_area :address, class:"search-field" %>
  </div>

  <div class="field">
    <%= f.label :telephone, class:"search-label" %>
    <%= f.text_field :telephone, class:"search-field" %>
  </div>

  <div class="field">
    <%= f.label :image, class:"search-label" %>
    <%= f.file_field :image, class:"search-field" %>
  </div>
  <br />

  <div class="actions waves-effect waves-light btn green darken-2">
    <%= f.submit %>
  </div>
<% end %>
<br />
