<div class="container">
  <div class="row">
    <div class="blurred-back col s6" style="padding-left: 50px; padding-right: 50px;">
      <h1>New Review</h1>

      <%= render 'form', review: @review %>

      <%= link_to 'Back', post_path(@post) %>
    </div>
  </div>
</div>
